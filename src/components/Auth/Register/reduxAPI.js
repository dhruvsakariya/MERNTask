// Promise
// fetch request ( to laravel )
// axios request ( get , post , put , patch ...)
// firestore requests
// cloud function requests


// import axios from "../../../../axios/axiosInstance";

export function registerUser(email, password) {
  // return createUserWithEmailAndPassword(auth, email, password);
}

export function assignRole(uid, firstName, lastName, email, rememberMe, role) {
  // const assignRole = httpsCallable(functions, "assignRole");
  // return assignRole({ uid, firstName, lastName, email, rememberMe , role });
  // return axios.post("assignRole", {
  //   uid,
  //   firstName,
  //   lastName,
  //   email,
  //   rememberMe,
  // });
}
